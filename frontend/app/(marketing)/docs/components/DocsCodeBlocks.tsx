import type { MDXComponents } from 'mdx/types';

/**
 * Code block typography (block + inline `code`) is owned by
 * `.editorial .prose` in editorial.css. No overrides needed here.
 */
export const DocsCodeBlocks: MDXComponents = {};
