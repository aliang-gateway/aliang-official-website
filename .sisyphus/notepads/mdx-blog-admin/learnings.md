- Added migration `backend/migrations/0007_add_articles.sql` using SQLite-compatible additive style (`CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS`) to keep repeated test DB migration runs safe.
- Implemented article status constraint via `CHECK(status IN ('draft', 'published'))` and partial unique index `WHERE legacy_id IS NOT NULL` for optional legacy mapping uniqueness.
- Included indexing strategy for blog listing/query patterns: unique `slug`, filter on `status`, and sort/query support on `published_at`.
- Added `backend/internal/article/service.go` with reusable internal methods for create/update/get/list/publish/unpublish/delete and explicit domain errors (`ErrArticleNotFound`, `ErrDuplicateSlug`) to keep authorization concerns out of the data/service layer.
- Implemented slug uniqueness guard in service methods (pre-check + DB unique-constraint fallback mapping) so API handlers can receive deterministic conflict behavior without inspecting driver-specific errors.
- Public access behavior is enforced at service level via `GetArticleBySlug` and `ListPublishedArticles`, both restricted to `status='published'`, while admin editing uses unrestricted `GetArticleByID` / `ListArticles` with optional status filter.
- Added admin-only article CRUD/list handlers in `backend/internal/httpapi/routes.go` with explicit `RequireAdmin` route wrapping, slug regex validation (`^[a-z0-9]+(?:-[a-z0-9]+)*$`, max 128), required create fields (`slug`, `title`, `mdx_body`), and sanitized error mapping (`ErrDuplicateSlug`→409, `ErrArticleNotFound`→404, generic failures→500) to avoid leaking DB internals.
- For admin article read/update by slug across draft/published entries, route layer uses `ListArticles` + in-memory slug lookup helper because service `GetArticleBySlug` is intentionally public/published-only.
- Added public HTTP routes `GET /public/articles` and `GET /public/articles/{slug}` in `backend/internal/httpapi/routes.go` without auth middleware, following existing public route registration style.
- Public article list/detail response DTOs intentionally exclude admin-only fields (`created_by_user_id`, `updated_by_user_id`) and include `mdx_body` only in detail payload to keep list responses lean.
- Implemented deterministic newest-first list ordering at API layer by stable sorting mapped DTOs on `published_at` descending, while still relying on article service methods that already gate to published-only records.

- 2026-03-19: Refactored frontend/app/blog/page.tsx to fetch list data from /api/public/articles with cache "no-store", mapped API fields (slug/title/excerpt/cover_image_url/tag/published_at/read_time/author*), switched card and modal links to /blog/${slug}, and added guarded loading skeleton + empty/error states to avoid runtime crashes while preserving existing card layout semantics.
- 2026-03-19: Added slug detail route at frontend/app/blog/[slug]/page.tsx as a server component that fetches article by slug from backend public endpoint (`NEXT_PUBLIC_API_BASE_URL/public/articles/{slug}`), returns Next.js 404 via `notFound()` when upstream returns 404, and preserves existing detail layout (back link, cover, metadata, title, excerpt, footer).
- 2026-03-19: Introduced frontend/mdx-components.tsx with `useMDXComponents` mapping for h2/h3/p/ul/ol/a/code/pre/strong to preserve blog typography and safely render MDX body (no custom parser, no dangerouslySetInnerHTML, no remote component evaluation).
- 2026-03-19: Build currently blocked by existing dynamic route ambiguity between `/blog/[id]` and `/blog/[slug]` in Next.js App Router. New slug page implementation is ready, but full build cannot pass until one route pattern is disambiguated (e.g., remove/rename legacy route in follow-up task).

- 2026-03-19: Added `frontend/app/admin/articles/page.tsx` as a client admin console using session-token auth from `localStorage` (`session_token`) and forwarding `Authorization: Bearer <token>` to all `/api/admin/articles*` proxy calls for list/detail/create/update/delete/publish/unpublish flows.
- 2026-03-19: Implemented unified create/edit MDX form with required `title`/`slug`/`mdx_body`, auto-slug generation from title in create mode, explicit draft/published status control, and edit-mode prefill via `GET /api/admin/articles/{slug}`.
- 2026-03-19: Implemented article list table with status badge, published date formatting, and per-row actions (`Edit`, `Publish`/`Unpublish`, `Delete`) using confirmation dialogs and per-row loading indicators to prevent duplicate submissions.
- 2026-03-19: Added clear state UX on admin page: initial/loading/empty/error/success states, plus explicit unauthorized blocked workflow messaging when API returns 401/403 or token is missing.
- 2026-03-19: Frontend verification passes: `npm -C frontend run build` successfully builds route `/admin/articles` and related admin API route handlers.

- 2026-03-20 (F3 QA): Automated gates passed (`go test ./...`, `npm -C frontend run build` after installing frontend deps), but runtime manual QA failed critical flows: `/blog` showed upstream error state, `/blog/{slug}` returned 404, and `/blog/{legacy-id}` redirected to a slug page that also 404s.
- 2026-03-20 (F3 QA): `/admin/articles` no-token boundary correctly blocked in UI, but non-admin boundary could not be validated as expected 403 because proxy returned 502 (`invalid json response from upstream`), indicating backend/article route availability mismatch in running environment.


- 2026-03-20 (F3 rerun): Revalidated live stack after backend mismatch correction; `/api/public/articles` now returns JSON `{"articles":[]}` through frontend proxy and `/blog` renders normal empty-state UI (no upstream JSON parse error).
- 2026-03-20 (F3 rerun): Browser-level QA completed via Chrome DevTools MCP fallback because Playwright MCP failed to launch in this environment (persistent Chrome session conflict); evidence captured under `.sisyphus/evidence/f3-rerun-*.png`.
- 2026-03-20 (F3 rerun): Non-admin authorization boundary confirmed end-to-end: real registered user token (default role `user`) against `/api/admin/articles` returns `403 {"error":"admin role required"}`, while missing token remains `401`.
- 2026-03-20 (F3 rerun): Legacy route `/blog/1` correctly issues 308 redirect to mapped slug `/blog/gpt4o-vs-claude35`; destination currently 404 due absent published article dataset, not runtime/proxy failure.
- 2026-03-20: Added backend registration toggle `register.require_email_verification` (nullable bool pointer in `backend/internal/config/config.go` + default `true` behavior handled in `user.NewServiceWithOptions`) to preserve existing verification-required flow when config is absent.
- 2026-03-20: Wired toggle through `backend/main.go` -> `user.ServiceOptions.RequireEmailVerification`, and updated `Service.Register` to conditionally create verification token/email outbox + send verification email only when toggle is enabled; when disabled, users are created with `email_verified=1` and can login immediately.
- 2026-03-20: Extended register response contract with `require_email_verification` and updated frontend register UX (`frontend/app/register/page.tsx`) to render/require verification step only when backend indicates it; success messaging now branches by toggle.
- 2026-03-20: Added frontend proxy fallback normalization in `frontend/app/api/auth/register/route.ts` so successful register responses default `require_email_verification` to `true` if older upstream payloads omit the field, keeping backward compatibility.
- 2026-03-20 (QA fix): `user.Service.Login` must gate `ErrEmailNotVerified` behind `requireEmailVerification`; otherwise disabling verification only affects new registrations and still blocks legacy unverified accounts from logging in.
