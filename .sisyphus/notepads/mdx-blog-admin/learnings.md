- Added migration `backend/migrations/0007_add_articles.sql` using SQLite-compatible additive style (`CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS`) to keep repeated test DB migration runs safe.
- Implemented article status constraint via `CHECK(status IN ('draft', 'published'))` and partial unique index `WHERE legacy_id IS NOT NULL` for optional legacy mapping uniqueness.
- Included indexing strategy for blog listing/query patterns: unique `slug`, filter on `status`, and sort/query support on `published_at`.
- Added `backend/internal/article/service.go` with reusable internal methods for create/update/get/list/publish/unpublish/delete and explicit domain errors (`ErrArticleNotFound`, `ErrDuplicateSlug`) to keep authorization concerns out of the data/service layer.
- Implemented slug uniqueness guard in service methods (pre-check + DB unique-constraint fallback mapping) so API handlers can receive deterministic conflict behavior without inspecting driver-specific errors.
- Public access behavior is enforced at service level via `GetArticleBySlug` and `ListPublishedArticles`, both restricted to `status='published'`, while admin editing uses unrestricted `GetArticleByID` / `ListArticles` with optional status filter.
- Added admin-only article CRUD/list handlers in `backend/internal/httpapi/routes.go` with explicit `RequireAdmin` route wrapping, slug regex validation (`^[a-z0-9]+(?:-[a-z0-9]+)*$`, max 128), required create fields (`slug`, `title`, `mdx_body`), and sanitized error mapping (`ErrDuplicateSlug`→409, `ErrArticleNotFound`→404, generic failures→500) to avoid leaking DB internals.
- For admin article read/update by slug across draft/published entries, route layer uses `ListArticles` + in-memory slug lookup helper because service `GetArticleBySlug` is intentionally public/published-only.
- Added public HTTP routes `GET /public/articles` and `GET /public/articles/{slug}` in `backend/internal/httpapi/routes.go` without auth middleware, following existing public route registration style.
- Public article list/detail response DTOs intentionally exclude admin-only fields (`created_by_user_id`, `updated_by_user_id`) and include `mdx_body` only in detail payload to keep list responses lean.
- Implemented deterministic newest-first list ordering at API layer by stable sorting mapped DTOs on `published_at` descending, while still relying on article service methods that already gate to published-only records.
